PORT=4000
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/locker_management?retryWrites=true&w=majority
CLIENT_ORIGIN=http://localhost:5173
OFFICER_PASSWORD=<shared password officers use to log into the dashboard>
JWT_SECRET=<random string, e.g. output of `openssl rand -hex 32`>
